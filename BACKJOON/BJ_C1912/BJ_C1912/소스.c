#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main() {
	int n;
	int input;
	int pre, last;
	int sum;
	scanf("%d", &n);
	scanf("%d", &pre);
	scanf("%d ")

	for (int i = 1; i < n; i++) {
		pre = input;
		scanf("%d", &input);
		if() 
		
	}
	return 0;
}